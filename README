This is a compiler for the tiger language

# Quick Start
| $ mk
| $ ./o.parser
| hi
| ^D
| (var (simple-var hi))
| $
| $ ./interact.sh ./o.parser hi
| (simple-var hi)
| $
| $ ./interact.sh ./o.parser
| hi
| (var
|  (simple-var hi))
| let var x = hi.there[fred] in end
| (let
|  (
|   (var x #f
|    (var
|     (subscript-var
|      (field-var
|       (simple-var hi) there)
|      (var
|       (simple-var fred))))))
|  (seq))
| ^D
| $

# Building
We are using plan9 mk.  It is available on most Linux distros as a
package named 'mk' or in either the '9base' or 'plan9port' packages.
To build all the executables, run:

	| mk
